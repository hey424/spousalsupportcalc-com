# spousalsupportcalc.com — Astro pilot

Migration from Hostinger Horizons (CSR React SPA) to Astro (SSG with real HTML output).

Auto-deploys to Cloudflare on git push to main.

```bash
npm install
npm run dev
npm run build
```
